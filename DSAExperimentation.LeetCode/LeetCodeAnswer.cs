namespace DSAExperimentation.LeetCode;

// LeetCode's own reporting conventions, shared by problems that otherwise have
// nothing to do with each other. Like Domain.Modular's 1e9+7, these values are
// fixed by the problem set rather than by any algorithm - which is why they are
// declared once here at the root of the LeetCode tier instead of being copied
// into each problem folder that happens to need one.
internal static class LeetCodeAnswer
{
    // Returned by count/distance problems when no valid answer exists.
    public const int None = -1;
}
