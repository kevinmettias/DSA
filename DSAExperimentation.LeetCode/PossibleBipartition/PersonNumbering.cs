namespace DSAExperimentation.LeetCode.PossibleBipartition;

// LeetCode 886 numbers people from 1, so index 0 of either prepared input is an
// unused placeholder and every walk starts at the first real person. Separate from
// the solution because the graph that materializes the input has to build that
// placeholder slot around the same numbering the strategies walk.
internal static class PersonNumbering
{
    public const int First = 1;
}
